import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: Center(
        child: Icon(Icons.vibration, size: 100, color: Colors.blue),
      ),
    ),
  ));
}